{
  "name": "screeps",
  "module": "src/index.ts",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/screeps": "^3.3.6",
    "vite": "^4.5.0"
  },
  "scripts": {
    "build": "vite build",
    "build:bun": "bun build src/index.ts --target node --outfile main.js --minify"
  }
}